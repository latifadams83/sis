<% if object.errors.any? %>
    <div class="w3-alert w3-small" id="error_explanation">
      <p><%= pluralize(object.errors.count, "error") %> prohibits this object from being saved:</p>

      <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
<% end %>
